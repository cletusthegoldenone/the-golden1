# Paste into Railway → Web service (Root Directory = cletus-frontend)
# Do NOT set PORT

NODE_VERSION=20
NODE_ENV=production
NEXT_PUBLIC_SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# If proxying unmatched /api/* routes from Next to the API service:
API_BASE_URL=https://YOUR-API-SERVICE.up.railway.app

# AI routes only:
GEMINI_API_KEY=

# DB guidance:
# - If using Postgres, set a private DATABASE_URL in the service that connects to Postgres.
# - Do not set PERSISTENCE_ADAPTER on the web service.
# - Use PERSISTENCE_ADAPTER=file only on the API service when not using Postgres.
